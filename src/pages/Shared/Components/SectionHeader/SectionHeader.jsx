

const SectionHeader = ({heading}) => {
    return (
        <div>
            <p className="font-bold text-black text-3xl capitalize py-10">{heading}</p>
        </div>
    );
};

export default SectionHeader;